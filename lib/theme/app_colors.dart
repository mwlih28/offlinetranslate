import 'package:flutter/material.dart';

/// UYGULAMANIN RENK PALETİ.
///
/// Tasarım dili: koyu lacivert zemin + bembeyaz/açık kartlar + turuncu
/// vurgu + yeşil "çevrimdışı" rozeti. Tüm widget'lar renkleri buradan
/// alır — palet tek yerden değiştirilebilir.

/// Ana zemin (koyu lacivert).
const kNavy = Color(0xFF1F2D45);

/// Zeminden bir tık açık yüzeyler (ayar kartları, sheet zemini, satırlar).
const kNavyLight = Color(0xFF2B3B59);

/// Dil seçim çipleri gibi ara ton yüzeyler.
const kSlateChip = Color(0xFF3D4E6E);

/// Kaynak metin kartı (bembeyaz).
const kCardWhite = Color(0xFFFFFFFF);

/// Çeviri sonucu kartı (açık lavanta/gri).
const kCardLavender = Color(0xFFE7EBF4);

/// Beyaz/açık kartların üzerindeki koyu metin rengi.
const kInkDark = Color(0xFF1C2537);

/// Açık kartlardaki ikincil (soluk) metin/ikon rengi.
const kInkMuted = Color(0xFF5A6478);

/// Ana vurgu rengi (butonlar, aktif sekme, kamera FAB tarzı öğeler).
const kAccentOrange = Color(0xFFF97316);

/// "Çevrimdışı Modu Aktif" rozeti zemin/yazı renkleri.
const kGreenBadgeBg = Color(0xFF9FE7B9);
const kGreenBadgeInk = Color(0xFF14532D);
